/**
 * 任务：使用 instanceof 运算符判断指定对象是否为特定类的一个实例
 */
class Person{}
class Students extends Person{}
class Sch extends Students{}
public class Demos{
    public static void main(String[] args) {
        // 请在下面的Begin-End之间按照注释中给出的提示编写正确的代码
        /********* Begin *********/
        // 创建一个Students对象，判断该对象是否是Person类的实例
        //如果是输出：true，否则为false
        Students sc=new Students();
        if(sc instanceof Person)System.out.println("true");
        else System.out.println("false");
        // 创建一个Sch对象，判断该对象是否是Person类的实例
        // 输出判断结果
        Sch ss=new Sch();
        if(ss instanceof Person)System.out.println("true");
        else System.out.println("false");
        // 创建一个Person对象，判断该对象是否是Students类的实例
        // 输出判断结果
        Person faQ=new Person();
        if(faQ instanceof Students)System.out.println("true");
        else System.out.println(false);
        /********** End **********/
    }
}

