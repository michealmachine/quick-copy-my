/**
 * 定义输出考试学生的人数及姓名的方法，方法名为 print，传参的类型为String，无返回值。
 */
public class Student {

	// 请在下面的Begin-End之间按照注释中给出的提示编写正确的代码
	/********** Begin **********/
    public static void print(String... args){
        int n = args.length;
        System.out.println("本次参加考试的有" + n +"人，名单如下：");
        for (int i = 0;i < n;i ++){
            System.out.println(args[i]);
        }
    }
	/********** End **********/
}